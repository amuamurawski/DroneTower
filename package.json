{
  "name": "dronetower-frontend-tooling",
  "private": true,
  "description": "Tooling JS dla kart Lovelace DroneTower: eslint + testy node:test (bez zależności runtime).",
  "license": "MIT",
  "scripts": {
    "lint": "eslint",
    "test": "node --test tests/frontend/*.test.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "eslint": "^9.14.0",
    "globals": "^15.12.0"
  }
}
